import React from 'react'
import '../styles/button.css';

const Button = () => {
  const postComment = {
    
  }

  return (
    <div>
        <button id="button"
        onClick={postComment}
        >Post</button> 
    </div>
  )
}

export default Button;